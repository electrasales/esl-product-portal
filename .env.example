NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Never prefix this with NEXT_PUBLIC_ -- it bypasses all
# security rules and must never reach the browser. Needed for the admin
# "Users" page to create accounts. Supabase dashboard -> Project Settings
# -> API -> "service_role" secret.
SUPABASE_SERVICE_ROLE_KEY=
